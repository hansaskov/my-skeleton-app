/*place global styles here */
html,
body {
	@apply h-full overflow-hidden;
	background-image: radial-gradient(
			at 0% 100%,
			rgba(var(--color-secondary-500) / 0.33) 0px,
			transparent 33%
		),
		radial-gradient(at 110% 98%, rgba(var(--color-error-500) / 0.33) 0px, transparent 33%);
}
